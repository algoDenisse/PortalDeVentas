%br
=form_tag indexRegularUsers_path, :method => 'get' do
  %div.formStyle
    %p
      = text_field_tag :search, params[:search]
      = select("", "category_id", Category.pluck(:name, :id))
      = submit_tag 'Search', :name => nil
    .row
      - @articles.each do |article|
        .artStyle
          %h2= article.name
          //= image_tag article.image.url(:mini)
          = link_to image_tag(article.image.url(:mini), :border => 1), article


/ %br
/ %table
/   %thead
/     %tr
/       %th Name
/       %th Price
/       %th Likes
/       %th Description
/       %th
/       %th
/       %th

/   %tbody
/     %div
/       - @articles.each do |article|
/         %tr
/         %td= article.name
/         %td= article.price
/         %td= article.likes
/         %td= article.description
/         %td
/         %td
/         %td= link_to 'Show', article
/         -if current_user
/           -if current_user.id == article.imageable.id
/             %td= link_to 'Edit', edit_article_path(article)
/             %td= link_to 'Destroy', article, :method => :delete, :data => { :confirm => 'Are you sure?' }